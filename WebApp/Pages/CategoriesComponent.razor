@page "/categories"
@inject NavigationManager NavManager
@inject IViewCategoriesUseCase ViewCategoriesUseCase

<h3>Manage Categories</h3>

@if (categories != null)
{
    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Description</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var category in categories)   
            {
                <tr>
                    <td>@category.Name</td>
                    <td>@category.Description</td>
                    <td><button type="button" class="btn btn-primary" @onclick="(() => EditCategory(category))">Edit</button></td>
                </tr>
            }
        </tbody>
    </table>
}

<br/>
<button type="button" class="btn btn-primary" @onclick="AddCategory">Add Category</button>

@code {
    private List<Category> categories;

    protected override void OnInitialized()
    {
        categories = ViewCategoriesUseCase.Execute().ToList();
    }

    private void AddCategory()
    {
        NavManager.NavigateTo("/addcategory");
    }

    private void EditCategory(Category category)
    {
        NavManager.NavigateTo($"/editcategory/{category.CategoryId}");
    }
}
